#include <bits/stdc++.h>

using namespace std;

int main() {
  int a,b,c,d;
  scanf("%d  %d  %d %d", &a,&b,&c,&d);
  if(a!=0)
    cout << 1 << "\n";
  if(b!=0)
    cout << 2 << "\n";
  if(c!=0)
    cout << 3 << "\n";
  if(d!=0)
    cout << 4 << "\n";
}